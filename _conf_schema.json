{
  "steam_api_key": {
    "description": "Steam Web API Key",
    "type": "string",
    "hint": "在 https://steamcommunity.com/dev/apikey 申请",
    "obvious_hint": true
  },
  "steam_ids": {
    "description": "要监控的 SteamID64（英文逗号分隔）",
    "type": "string",
    "default": ""
  },
  "push_targets": {
    "description": "推送目标会话ID（unified_msg_origin，英文逗号分隔）",
    "type": "text",
    "default": "",
    "hint": "可在群里执行 /steam_monitor_bind 自动写入，或在此手动维护"
  },
  "poll_interval_sec": {
    "description": "基础轮询间隔（秒）",
    "type": "int",
    "default": 60
  },
  "online_only": {
    "description": "仅在离线->在线时推送（true）",
    "type": "bool",
    "default": true
  },
  "image_proxy_prefix": {
    "description": "图片中转前缀（用于头像/游戏图标）",
    "type": "string",
    "default": "https://images.weserv.nl/?url=",
    "hint": "留空为直连；可用 {url} 或 %s 占位，例如 https://images.weserv.nl/?url= 或 https://your-proxy/fetch?url={url}"
  },
  "allow_dns_private_for_allow_domains": {
    "description": "允许对白名单域名跳过 DNS 私网解析拦截",
    "type": "bool",
    "default": true,
    "hint": "用于修复部分网络环境下 Steam 官方域名被 DNS 污染/劫持为私网地址导致的误拦截；关闭后将恢复严格拦截"
  }
}